# Cloudflare Pages Build Settings

## Required Settings for Next.js 16

### Build Configuration
- **Framework preset**: Next.js (Static HTML Export) 또는 None 선택
- **Build command**: `npm run build`
- **Build output directory**: `.next` 또는 비워두기 (자동 감지)
- **Root directory**: `/` (기본값)

### Environment Variables
필요한 환경 변수가 있다면 Settings > Environment variables에서 추가:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### 문제 해결
1. Cloudflare Pages 대시보드 접속
2. mni-archive-v2 프로젝트 선택
3. Settings > Builds & deployments
4. Build settings 수정:
   - Build output directory를 비워두거나 `.next` 입력
   - Framework preset을 "Next.js" 선택

### Next.js 16 호환성
Next.js 16은 Cloudflare Pages에서 실험적 지원 상태입니다.
권장 옵션:
1. Next.js 15로 다운그레이드 (안정적)
2. Framework preset을 "None"으로 설정하고 수동 구성
3. Vercel로 배포 (Next.js 16 완벽 지원)

### 현재 상태
- ❌ Output directory 에러 발생
- ✅ 빌드 자체는 성공
- ⚠️ Next.js 16 호환성 문제 가능

